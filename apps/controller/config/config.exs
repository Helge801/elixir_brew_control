# This file is responsible for configuring your application
# and its dependencies with the aid of the Mix.Config module.
use Mix.Config

config :controller, :fermentor_1_target, 68
config :controller, :fermentor_2_target, 70
config :controller, :fermentor_lower_tolerance, 1
config :controller, :fermentor_upper_tolerance, 1
config :controller, :internal_upper_tolerance, 1
config :controller, :internal_lower_tolerance, 3
config :controller, :internal_offset, -2
